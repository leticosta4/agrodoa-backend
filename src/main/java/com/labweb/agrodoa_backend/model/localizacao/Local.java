package com.labweb.agrodoa_backend.model.localizacao;

import jakarta.persistence.Embeddable;

//@Embeddable //é uma classe embutida, não é uma tabela no banco então não será tratada como entidade
public class Local {
    //ver ainda como modelar
}
